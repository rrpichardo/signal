# Copy this to .env and fill in your real key.
# IMPORTANT: .env is NOT auto-loaded by Signal Stream. Either:
#   1. export GROQ_API_KEY=<value> in your shell, or
#   2. source .env yourself before running (e.g., `set -a && source .env && set +a`)
GROQ_API_KEY=gsk_your_key_here
